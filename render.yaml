services:
  - type: web
    name: bhu-drishti-api
    runtime: node
    plan: free
    buildCommand: npm ci --omit=dev && npm run build:server
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
