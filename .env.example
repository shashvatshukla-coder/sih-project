# Frontend: set this in Vercel after the Render backend is live.
VITE_API_BASE_URL=http://localhost:5000/api

# Backend: optional comma-separated browser origins. If unset, all origins are allowed.
CORS_ORIGINS=http://localhost:5173

# Render provides PORT automatically. This value is only for local runs.
PORT=5000
